<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AardvArchive!</title>
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2">
      <h1>
        <a href="/">AardvArchive</a>
      </h1>
      <nav>
        <a href="/views/homepage.handlebars">Home</a>

        <!-- ADD SEARCH FUNCTION -->

        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/views/profile.handlebars">Profile</a> |
        <a href="/views/homepage.handlebars">Logout</button>
        <a href ="/views/postdelete.handlebars">Post</a>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
